To install the extension, first unzip the file, then open up chrome or whatever chromium-based web browser you normally use. 
From there open up the link "chrome://extensions" and toggle in the top right corner "developer mode"
Then, click load unpacked and select the unzipped file and it will be ready to use right in your browser!